# Wispwood

GitHub Pages is the share link: https://j6sistek-ui.github.io/Wispwood/

After **every** game change (engine, overlay, audio, assets, menus), run:

```
bash /workspace/scripts/push-github.sh "short commit message"
```

Do not wait for the user to ask. Build Pages, copy the cache hashes, commit, and push.

Do not commit grok-pwa internals, `AGENTS.md`, `.grok/`, or `attachments/`.
